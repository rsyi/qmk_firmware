#pragma once

// Tap-holds (Esc/Ctrl, Q/Alt, J/Gui, Space/Lower, Bspc/Raise) always resolve a
// quick tap-then-hold as the hold action instead of auto-repeating the tap key —
// same behavior the planck keymap uses.
#define QUICK_TAP_TERM 0
